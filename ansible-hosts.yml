- !policy
  id: ansible-apps
  owner: !group managers
  body:
    - !group
      id: ansible-apps
    - &hosts
      - !host
        id: ansible-host
        annotations:
          description: Global host identity for Ansible
          authn/api-key: true
    - !grant
      role: !group
      members: *hosts